// Declaração de variáveis de números
var idade = 25;
var altura = 1.75;

// Operações com variáveis de números
var soma = idade + 5;
var media = (idade + 10) / 2;

console.log(soma); // Saída: 30
console.log(media); // Saída: 17.5

// Declaração de variáveis de string
var nome = "João";
var sobrenome = 'Silva';

// Concatenação de strings
var nomeCompleto = nome + ' ' + sobrenome;

console.log(nomeCompleto); // Saída: João Silva

// Declaração de variáveis de string
var nome = "João";
var sobrenome = 'Silva';

// Concatenação de strings
var nomeCompleto = nome + ' ' + sobrenome;

console.log(nomeCompleto); // Saída: João Silva

// Declaração de variáveis de array
var frutas = ['maçã', 'banana', 'laranja'];
var numeros = [1, 2, 3, 4, 5];

console.log(frutas[0]); // Saída: maçã
console.log(numeros.length); // Saída: 5

// Declaração de variáveis de objeto
var pessoa = {
    nome: 'Maria',
    idade: 30,
    cidade: 'São Paulo'
};

console.log(pessoa.nome); // Saída: Maria
console.log(pessoa['idade']); // Saída: 30

// Declaração de variáveis undefined e null
var x; // undefined
var y = null; // null

console.log(x); // Saída: undefined
console.log(y); // Saída: null
